# 보안 체크리스트

이 저장소의 학습용 점검표이자, 실제 서비스(미즈톡톡 등)로 옮길 때 쓸 기준입니다.
일반론이 아니라 **이 프로젝트 맥락**에 맞춘 항목만 담았습니다.

---

## 0. 가장 먼저 — 인증과 인가는 다르다

백엔드 보안 사고의 압도적 1위가 이 둘을 혼동하는 것입니다.

| | 질문 | 예 |
|---|---|---|
| **인증** (Authentication) | 너 누구야? | 로그인 |
| **인가** (Authorization) | 너 **이걸** 봐도 돼? | 이 주문이 이 사람 것인가 |

로그인은 정상적으로 통과했는데 남의 데이터가 나오는 경우가 여기서 생깁니다.

```
GET /api/orders/1234   → 내 주문. 정상
GET /api/orders/1235   → 남의 주문이 그냥 나옴   ← 사고
```

해킹 기술이 필요 없습니다. 주소창의 숫자만 바꾸면 됩니다.
이 유형을 **IDOR**(Insecure Direct Object Reference)라고 부릅니다.

**기능을 추가할 때마다 물어야 할 한 문장:**
> "URL이나 요청 값을 바꿔서 남의 데이터에 닿을 수 있는가?"

---

## 1. 키 취급

- [ ] `sb_secret_...` (Secret key)가 프론트엔드 코드에 없는가
- [ ] `.env`가 `.gitignore`에 있는가
- [ ] 커밋 전 `git diff`로 키·비밀번호가 섞이지 않았는지 확인했는가
- [ ] 노출된 적 있는 키는 **재발급**했는가 (지우는 것만으로는 부족 — git 이력에 남음)

> GitHub는 봇이 24시간 스캔합니다. 실수로 올린 키는 **수 분 안에** 사용됩니다.
> Publishable key(`sb_publishable_...`)는 공개돼도 정상입니다. 보호는 RLS가 담당합니다.

---

## 2. RLS 정책 (Supabase)

- [ ] 대상 테이블에 RLS가 **켜져** 있는가
- [ ] 정책이 **명령별**(SELECT/INSERT/UPDATE/DELETE)로 분리돼 있는가 — `ALL` 하나로 퉁치지 않았는가
- [ ] 열어둘 필요가 없는 명령은 **정책을 만들지 않았는가** (정책 없음 = 차단)
- [ ] `using (true)` / `with check (true)`가 남아 있다면, 그게 의도한 것인가
- [ ] 로그인 도입 후에는 `auth.uid() = user_id` 조건이 들어갔는가
- [ ] 느슨한 기존 정책을 **먼저 지웠는가** (정책 여러 개는 OR로 합쳐짐 — 안 지우면 조여지지 않음)

**RLS를 쓰는 이유:** 인가 검사를 API마다 직접 짜면 하나만 빠뜨려도 뚫립니다.
RLS는 DB가 직접 막기 때문에 **빠뜨릴 수가 없는 구조**입니다.

---

## 3. 사용자 입력

- [ ] 화면에 출력할 때 `textContent`를 썼는가 (`innerHTML` 아님) — XSS 방지
- [ ] 쿼리에 문자열을 이어붙이지 않았는가 — SQL Injection 방지
  (Supabase 클라이언트·ORM·파라미터 바인딩을 쓰면 대부분 자동 방어됨)
- [ ] 파일 업로드가 있다면, 확장자·MIME·용량을 서버에서 검사하는가
- [ ] **프론트엔드 검사만 믿지 않는가** — 브라우저 검사는 우회 가능. 서버에서 반드시 다시 검사

---

## 4. 비밀번호·인증

- [ ] 비밀번호를 **직접 저장·해싱하지 않는가** → Supabase Auth 등 검증된 것을 쓴다
- [ ] 관리자 화면에 접근 제어가 걸려 있는가 (URL만 알면 열리지 않는가)
- [ ] 세션·토큰 만료가 설정돼 있는가

> 비밀번호 저장은 직접 구현하면 안 되는 영역입니다. 평문·MD5·자체 암호화 전부 사고 원인입니다.

---

## 5. 개인정보 — 미즈톡톡 맥락에서 특히 중요

임산부·육아 서비스는 **건강 관련 정보**가 섞일 수 있습니다.
출산예정일, 임신주차, 아이 생년월일 등이 개인정보보호법상 **민감정보로 해석될 여지**가 있습니다.

- [ ] 수집 중인 개인정보 **목록**이 정리돼 있는가
- [ ] 그중 민감정보에 해당할 항목이 무엇인지 검토했는가
- [ ] 꼭 필요한 항목만 받고 있는가 (안 받는 것이 가장 강력한 보안)
- [ ] 로그·에러 메시지에 개인정보가 찍히지 않는가
- [ ] 외부 서비스(분석 도구, 광고 픽셀)로 개인정보가 새어나가지 않는가

> ⚠️ **이건 코드로 해결되는 문제가 아닙니다.** 별도 동의·암호화·접근기록 요구가 따라오고,
> 유출 시 과징금 규모가 다릅니다. 실서비스 적용 전 **법무 검토가 필요한 영역**입니다.

---

## 6. 배포 전

- [ ] 디버그 출력에 민감한 정보가 없는가
- [ ] 테스트용으로 열어둔 권한을 원복했는가
- [ ] 롤백할 수 있는가 (git 이력이 남아 있는가)
- [ ] 실서비스라면 — **백엔드 개발자에게 보안 관점 리뷰를 받았는가**

---

## 7. 서버를 직접 운영하게 되면 (지금은 해당 없음)

- [ ] DB 포트가 인터넷에 열려 있지 않은가 (`localhost`만 허용)
- [ ] SSH는 키 인증만 허용하고 root 직접 로그인은 차단했는가
- [ ] 방화벽에서 필요한 포트만 열었는가
- [ ] 자동 보안 업데이트가 켜져 있는가
- [ ] **백업이 다른 위치에** 저장되는가 (같은 서버에 두면 서버가 죽을 때 같이 죽음)
- [ ] 백업을 실제로 **복구해봤는가** ← 해본 적 없는 백업은 백업이 아님

---

## 검증 습관

만들고 나서 반드시 되물을 것:

```
이 코드에서 보안상 문제될 부분 짚어줘.
특히 인가 검사가 빠진 곳이 있는지 봐줘.
```

방어를 만들었으면 **공격도 해볼 것**:

```
방금 만든 정책, 우회할 방법 있어?
```

> 방어만 배우면 감이 안 옵니다. 조건을 빼면 실제로 남의 데이터가 나오는 걸
> 눈으로 봐야 코드를 보는 눈이 생깁니다. 테스트 프로젝트에서는 마음껏 뚫어보세요.

---

## 한계 인지

Claude는 **읽은 코드**만 판단할 수 있습니다.
"이 함수가 위험하다"는 말할 수 있어도 "시스템 전체가 안전하다"는 말할 수 없습니다.
전수 검사가 필요하면 명시적으로 요청하고, 실서비스는 사람의 리뷰를 함께 받으세요.
